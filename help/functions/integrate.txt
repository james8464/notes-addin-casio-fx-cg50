Args: integrate(expr,var) or integrate(expr,var,a,b)
Required: expr, var
Does: Finds an antiderivative, or a definite integral when limits are supplied.
Examples: integrate(3*x^2,x); integrate(sin(x),x,0,pi)
Working: Shows substitution, parts, table, and limit-substitution lines where routed.
