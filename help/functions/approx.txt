approx
Args: approx(expr)
Required: expr: exact number or expression.
Optional: Display precision follows calculator settings.
Does: Returns a decimal approximation without changing the exact expression rules.
Working: Native KhiCAS output; no extra working lines.
Checks: Approximate output is not used as proof unless an exact check is unavailable.
Examples: approx(1/3)
