exp
Args: exp(expr)
Required: expr: exponent expression.
Optional: Use e^expr as equivalent input if parser accepts it.
Does: Returns e raised to expr.
Working: Apply exponent laws when simplifying or solving exponential equations.
Checks: Log/exp inverse steps are checked with domain restrictions.
Examples: exp(1)
