# Evidence

- `CASP3.g3a`: rebuilt into `calculator_files/`, metadata and size checks passed in `./compile`.
- `CSCALC.g3a`: rebuilt into `calculator_files/`, metadata and size checks passed in `./compile`.
- Focused engine tests and multi-app suite passed after the changes.
