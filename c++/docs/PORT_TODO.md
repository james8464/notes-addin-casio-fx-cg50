## Casio fx-CG50 g3a port — remaining tasks (ordered)

Status now (already done):
- C++ core: `normalize_text()`, tokenizer, `parse_expr()`, minimal `simplify()`, `format_expr()` parity smoke-checked vs Python.
- Exam-style printer: `format_equation_human_readable()` (ported from `python/src/shared_formatting.py`).
- Host CLI: `addin/host/build/casio_host` for fast parse/format tests.
- Golden tools: `tools/golden/generate_fixtures.py` + `tools/golden/compare_expr_format.py`.
- Boolean module: C++ port + add-in menu (simplify/NAND/NOR/prove) working as MVP UI.

This file lists what still must be done to claim **FULL conversion** (all programs + all functionality).

---

## Phase 0 — repo hygiene + tooling (must do first)

0) **Fix hardware-visible add-in UX**
- [x] Set a non-empty `.g3a` menu name so the calculator menu does not show a blank tile.
- [x] Add selected/unselected CG icon PNGs so the add-in is not a black box.
- [x] Ensure every visible add-in menu item runs real bounded device functionality or reports a scoped unsupported input.
- [x] Add KhiCAS-inspired device UI chrome: status strip, dense catalog rows, selection bar, scrollbar, and soft-key bar.
- [ ] Add a hardware screenshot pass after installing each release candidate, checking icon visibility, menu labels, text density, and long worked-solution readability.

1) **Ignore local junk**
- Add root `.gitignore` entries for:
  - `.DS_Store`, `.idea/`, `python/.DS_Store`
  - `python/Tests.txt` (generated by `textutil`)
- Confirm `addin/.gitignore` already excludes `host/build/` and add-in builds.

2) **Make “host C++ build” reproducible**
- Add script `tools/build_host.sh` that builds host binary using the pip-installed cmake path (or finds `cmake` on PATH).
- Output binary path should be stable: `addin/host/build/casio_host`.

3) **Pin “cmake via pip” in docs**
- Add note in `addin/README.md` that if `brew` absent, use:
  - `python3 -m pip install --user cmake`
  - add `/Users/james/Library/Python/3.14/bin` to PATH (or use full path).

---

## Phase 1 — fxSDK + gint toolchain (required for real `.g3a` builds)

4) **Install fxSDK toolchain**
- Preferred: Linux/WSL or a Linux VM (most reliable).
- Using fxSDK docs:
  - install via GiteaPC (recommended by fxSDK): `giteapc install Lephenixnoir/fxsdk`
  - install dependencies + `sh-elf-gcc`, `gint`, `fxlibc`, `OpenLibm`, etc.
- Verify with a hello add-in:
  - `fxsdk new Hello && cd Hello && fxsdk build-cg`
  - confirm `.g3a` produced.

5) **Make this repo buildable with fxSDK**
- From `addin/` run:
  - `fxsdk build-cg`
- Fix any missing fxSDK CMake functions / icon metadata requirements.

6) **Install workflow**
- Pick one:
  - Manual copy `.g3a` to calculator USB storage.
  - `fxsdk send-cg` (Linux+UDisks2).
- Add doc section: where to put `.g3a` so it shows in main menu.

---

## Phase 2 — UI foundation (needed before porting big modules)

7) **Replace current “menu via viewer + press key”**
- Implement a real menu widget:
  - highlight selection
  - Up/Down to move
  - EXE to select
  - EXIT to back
- Add “Home menu” in add-in:
  - Boolean
  - SUVAT
  - Derive
  - Integrate
  - Algebra
  - Trig

8) **Improve text input editor**
- Current `text_input()` is single-line and very limited key mapping.
- Extend mapping for math programs:
  - `x`, `y`, `pi`, parentheses, `^`, fraction `/`, `sqrt`, trig function typing
  - allow lowercase too (normalize to your expected behavior)
- Add cursor movement and insert (optional but strongly recommended).

9) **Output viewer improvements**
- [x] Add line wrapping/chunking for long exam-working lines.
- [x] Add page up/down keys (LEFT/RIGHT) in addition to row scrolling.
- [ ] Add optional horizontal pan only if real hardware shows wrapped chunks are still too cramped.

---

## Phase 3 — Core math engine parity (must be correct before big ports)

10) **Finish `simplify()` parity for `casio_core.py`**
Your C++ simplify currently covers only the minimal subset. The Python `casio_core.py` has more helpers used by modules:
- `depends()`
- `flat()` behavior parity
- any extra normal forms relied on by algebra/trig/int/derive code

Goal:
- For a large set of expressions, C++ `format_expr(parse_expr(x))` must match Python exactly.

11) **Add equation parsing helpers**
Many modules accept equation strings `lhs=rhs` and also accept `expr=0` implicit forms.
- Implement C++ helper:
  - `parse_equation_or_zero(text) -> (lhsNode, rhsNode)`
- Mirror Python behaviors from:
  - trig/algebra `parse_equation_or_zero()` implementations.

12) **Add rational overflow strategy**
Python uses big ints naturally. C++ currently uses 64-bit.
Decide one:
- Implement big integer (heavy).
- Or implement overflow detection + “fallback to double + mark inexact” for non-critical paths.
For full parity, big integer support is likely needed eventually.

---

## Phase 4 — Port remaining programs (big work, ordered)

### 4A) SUVAT (do first; smaller surface than trig/algebra)
13) **Port `python/src/Math/SUVATprogram.py` → C++ module**
- Create `addin/src/modules/suvat/`:
  - port AST usage (`num/sym/add/mul/div/pow/fn`) to C++ core
  - port solve flows and formatting logic
- Add add-in menu entry “SUVAT”.
- Inputs needed (match Python): whichever prompts SUVAT uses.
- Output: same “exam working” lines (use `format_exam_working()`).

14) **Golden tests for SUVAT**
- Extract a set of SUVAT questions from `Tests.rtf` mapping (or add a dedicated list).
- Ensure host-side runner can execute SUVAT module and compare to Python output.

### 4B) Derive
15) **Port `python/src/Math/deriveProgram.py`**
- This file is large; port in layers:
  - AST node constructors + guards (`EXAM_GUARD_*`)
  - derivative rules
  - tidy/compact output logic
- Add add-in menu entry “Derive”.

16) **Derive golden tests**
- Reuse `python/tests/test_specific_regressions.py` cases (especially compact syntax, abs, factor preferences).

### 4C) Integrate
17) **Port `python/src/Math/intProgram.py`**
- Method ladder: direct/u-sub/parts/trig/pfrac/div routes.
- “Non-elementary” explanations must match.
- Add add-in menu entry “Integrate”.

18) **Integrate golden tests**
- Use regression tests + RTF-mapped cases.

### 4D) Algebra
19) **Port `python/src/Math/algebraProgram.py`**
- Parsing + simplify + solve + transform modes.
- This will likely require expanding the C++ simplify engine a lot.
- Add add-in menu entry “Algebra”.

20) **Algebra golden tests**
- Use regression tests + RTF-mapped cases.

### 4E) Trig (last; biggest)
21) **Port `python/src/Math/trigProgram.py`**
- Keep route ladder structure.
- Maintain domain/branch messages (acute/obtuse etc.).
- Add add-in menu entry “Trig”.

22) **Trig golden tests**
- Use `python/tests/test_specific_regressions.py` trig tests + RTF bank.

---

## Phase 5 — “One `.g3a`” size/perf validation (must do before calling it finished)

23) **Check `.g3a` size**
- Build with `-Os` and check output `.g3a` size.
- If > ~2 MiB (risk of not showing), implement fallback:
  - store large tables/strings in external data file loaded at runtime
  - keep `.g3a` under limit

24) **Memory/performance tests**
- Stress-case expressions from trig/algebra/int/derive.
- Track: time per solve, max recursion depth, heap usage.

---

## Phase 6 — Testing strategy (best available)

### A) Host “golden diff” (main automation)
25) **Freeze Python oracle fixtures**
- Generate `tools/golden/rtf_cases.jsonl` (full) from `Tests.rtf`.
- Store in repo (or store generator + use CI artifact; choose one policy).

26) **Add host-side test runner**
- Add `tools/golden/diff_cases.py`:
  - reads JSONL cases
  - runs Python program (oracle)
  - runs C++ host runner for equivalent module/mode
  - compares outputs (exact or normalized comparison rules)

### B) On-device smoke tests (manual but necessary)
27) **Manual smoke checklist**
- For each menu item:
  - 5–10 representative inputs
  - verify no crash, output sensible, matches Python for key cases

28) **Optional: USB logging**
- Use fxlink + gint USB features to stream logs/screenshots for faster debugging (if feasible).

---

## Definition of “DONE”
You can call this “fully converted” only when:
- All modules available in add-in: Boolean, SUVAT, Derive, Integrate, Algebra, Trig
- Inputs accepted match Python conventions (including compact syntax)
- Outputs (working + Answer) match Python for a large fixture set
- Add-in stable on hardware (no frequent resets/hangs)
- `.g3a` visible in main menu (size under limit or external data workaround)
