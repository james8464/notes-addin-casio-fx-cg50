# Standard application layer protocols

| Protocol | Port(s) | Purpose |
| --- | --- | --- |
| FTP - file transfer protocol | 20 & 21 | Used for sending files between devices; Used for the connection between an FTP server and a client |
| SSH - secure shell | 22 | Used for remote management of devices |
| SSH - secure shell | 22 | Requires username + password |
| SSH - secure shell | 22 | Information is encrypted during transmission |
| SSH - secure shell | 22 | Essentially, you can run commands from another computer |
| HTTP - hypertext transfer protocol | 80 | i.e., for loading webpages |
| HTTPS - hypertext transfer protocol secure | 443 | Same as HTTP, but with encryption |
| POP3 - post office protocol 3 | 110 & 995 | Used for retrieving emails from an email server |
| SMTP - simple mail transfer protocol | 25 & 587 & 465 | Used for sending emails |

FTP = Method used for transferring files/data across a network, often the internet
- An Application Layer Protocol
- Files can be dragged from the folder structure of the remote site or server (SEE BELOW)
- FTP sites may also be used by software companies offering large updates, or by press photographers to upload their latest photographs to a remote newspaper headquarters.
- Most FTP sites require a username and password to authenticate the user, but some sites could be configured to allow anonymous use without the need to login.
1. Toolbar, 2. Quick connect bar, 3. Message log, 4. Local pane, 5. Remote pane, 6. Transfer queue

- Definition: an encrypted protocol that allows secure communication between nodes across a network

## SSH Clients

- pieces of software
- used to make a TCP connection to a remote port (via port 22)
- this connection allows commands for application-level protocols to be sent to the remote server

### Role of a Web Server

- Stores web pages (HTML files)
- Receives requests for those pages
- Responds to the requests by sending pages as text using HTTP/HTTPS

### Role of a Web Browser

- A web browser sends requests to a web server asking for web pages;
- It then receives the HTML text and all other resources (images, CSS, scripts) needed for the page;
- The browser downloads these files, interprets them, and renders the finished web page to the user;
